import { Router } from "express";
const router = Router();

// assignment create a Router here, set it up with the server
//  and create a POST route on  /api/contact 

router.post("/api/contact", (req, res) => {
    res.send();
});


export default router;
